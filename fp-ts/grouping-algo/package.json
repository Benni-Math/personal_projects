{
  "name": "grouping-algo",
  "version": "1.0.0",
  "description": "First draft: use double weighted (undirected) graph system with live addition and manual re-shuffling. One graph of students and one graph of groups     - Only option of either fixing/preferring group-size or group number     - First set max group-size and group-number and then set preference of       which is more important",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benedikt Arnarsson",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
