fn main() {
    let mut v: Vec<i32> = Vec::new();
    // using the vec! macro
    // let v  = vec![1, 2, 3]; 

    // adding new elements
    v.push(5);
    v.push(6);
    v.push(7);
    v.push(8);
}
