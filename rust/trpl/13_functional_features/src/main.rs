use functional_features::generate_workout;
use functional_features::generate_workout_rc;

fn main() {
    println!("Checkout the code!\n");

    generate_workout(4, 19);

    generate_workout_rc(26, 3);
}